{
  "name": "troy",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "bun build dashboard/app.ts --outdir dashboard/dist --minify",
    "test": "bun test"
  },
  "dependencies": {
    "@tanstack/query-core": "^5.62.0"
  },
  "devDependencies": {
    "@types/bun": "^1.1.0",
    "typescript": "^5.7.0"
  }
}